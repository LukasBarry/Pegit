<%= form_for([@meetup, @comment], url: meetup_comment_path) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :body, class:"sr-only" %><br>
    <%= f.text_area :body, class: "form-control", placeholder: "Comment", rows: 2 %>
  </div>
  <div class="field cant-see-me">
    <%= f.number_field :meetup_id, value: @meetup.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
